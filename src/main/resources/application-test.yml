spring:
  application:
    name: payment
  datasource:
    url: jdbc:h2:tcp://localhost/~/payment
    username: sa

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true